# {需求名称}-实施计划

> 使用说明：以本模板创建该需求唯一的实施计划。填写所有适用字段；删除不适用章节，或明确写出“不适用”及原因。不得保留无解释的占位内容。

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 待审核 / 审核通过 / 已替代 |
| 需求来源 | 原始需求文档路径或链接 |
| 目标项目 | 项目名称与根目录 |
| 调研基准 | 分支、提交、版本或调研日期；无法获得时说明 |
| 执行步骤生成状态 | 未生成（待审核）/ 待生成（已审核）/ 已生成 / 需重新生成 |
| 审核依据 | 待审核；或用户明确审核通过的会话说明与日期 |
| 最后更新 | 日期及变更原因 |

## 2. 范围与目标

### 2.1 实施目标

{概述要实现的结果，并引用原始需求的章节、编号、表格行或简短摘要。}

### 2.2 包含范围

- {范围项及需求引用}

### 2.3 不包含范围

- {明确排除项及原因}

## 3. 项目现状与约束

### 3.1 已核实现状

| 位置 | 当前能力或行为 | 与本需求的关系 | 证据 |
| --- | --- | --- | --- |
| `{已核实路径}` | {现状} | {复用、修改或限制} | {代码、配置、测试或文档依据} |

### 3.2 项目规范与约束

- {适用的项目规则、技术限制、兼容要求或外部条件}

### 3.3 可复用能力

- {已有模块、接口、数据结构、组件或测试工具}

## 4. 推荐实施方案

### 4.1 方案概述

{说明总体方案及主要数据流或调用链。}

### 4.2 关键决策

| 决策 | 选择 | 理由 | 替代方案及不采用原因 |
| --- | --- | --- | --- |
| {存在实质选择时填写} | {推荐选择} | {与需求及项目事实相连的理由} | {确有必要时填写} |

### 4.3 模块与文件变更

| 模块或位置 | 变更类型 | 计划变更 | 依据与影响 |
| --- | --- | --- | --- |
| `{已核实路径或“拟新增：路径”}` | 新增 / 修改 / 删除 / 配置 | {具体变更} | {需求引用、调用方或影响范围} |

### 4.4 接口与数据变化

{按实际需求描述接口契约、数据结构、状态变化、持久化或迁移。没有变化时明确说明。}

### 4.5 关键流程、异常与边界

1. {正常流程步骤}
2. {异常、权限、并发、幂等、空值或其他适用边界的处理}

### 4.6 兼容、迁移与回退

{仅在适用时描述兼容范围、迁移顺序、数据保护和回退条件；否则说明不适用。}

## 5. 验证与验收策略

| 验证层级 | 验证内容 | 方法或命令 | 通过标准 | 需求引用 |
| --- | --- | --- | --- | --- |
| {单元 / 集成 / 端到端 / 人工} | {待验证行为} | `{从项目核实的命令}` 或 {检查步骤} | {可观察结果} | {需求编号或位置} |

未实际执行的验证只能写为“计划执行”，不能标记为已通过。

## 6. 假设、风险与待确认事项

### 6.1 假设

| 编号 | 假设 | 依据 | 假设不成立时的影响 |
| --- | --- | --- | --- |
| A-01 | {可逆且已明确标注的假设} | {依据} | {影响} |

### 6.2 风险

| 编号 | 风险 | 影响 | 缓解或验证方式 |
| --- | --- | --- | --- |
| RK-01 | {风险} | {影响} | {措施} |

### 6.3 待确认事项

| 编号 | 问题 | 影响范围 | 状态或决定 |
| --- | --- | --- | --- |
| Q-01 | {只列会影响范围、规则、数据或总体方案的问题} | {受影响内容} | 待确认 / 已确认：{决定} |

## 7. 执行步骤生成建议与记录

第一阶段只记录步骤范围和依赖建议，不创建执行步骤文件、不添加无效链接，也不分配正式任务编号。用户明确审核通过并要求生成后，再把拟定文件名替换为实际链接并更新生成状态。正式生成时，实施计划文件名为 `{需求名称}-实施计划.md`，每项任务文件名为 `{需求名称}-任务-T01-执行步骤.md`。

| 顺序 | 拟定或已生成的执行步骤文件 | 负责范围 | 启动条件 | 前置任务 | 后续任务 | 是否可并行 | 生成状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 待审核后生成 | {拟负责范围} | 实施计划审核通过及{其他条件} | 无 | {待定} | 否 | 未生成（待审核） |

第二阶段生成后，每一项任务都必须拥有独立的可访问文件链接，并在此列出正式任务编号、前置任务、后续任务及推荐顺序。不要生成包含完整任务清单的总任务文件。

### 7.1 审核记录

| 日期 | 审核结果 | 用户修改或决定 | 对拆分建议的影响 |
| --- | --- | --- | --- |
| {日期} | 待审核 / 审核通过 | {修改、补充或明确决定} | {无 / 调整说明} |

## 8. 需求覆盖矩阵

| 需求引用 | 方案位置 | 拟执行范围 | 正式执行步骤 | 验证或验收方式 | 覆盖状态 |
| --- | --- | --- | --- | --- | --- |
| {原文编号或轻量引用编号} | {本计划章节} | {执行步骤生成建议中的范围} | 待审核后生成 | {验证项} | 方案已覆盖 / 阻塞：{原因} |

第二阶段生成执行步骤后，把“正式执行步骤”更新为实际 `T01`、`T02` 等编号及文件链接，并确认全部需求均已覆盖。

## 9. 变更记录

| 日期 | 变更来源 | 受影响内容 | 处理结果 |
| --- | --- | --- | --- |
| {日期} | {需求变更、项目变化或用户决定} | {章节与任务} | {定向修订说明} |
